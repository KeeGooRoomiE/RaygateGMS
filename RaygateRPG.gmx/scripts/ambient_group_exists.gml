///ambient_group_exists(name)
/*
 * Checks if a ambient AI group with the given name exists.
 *
 * name: The name of the group.
 *
 * Returns: true if the group exists, false otherwise.
*/

return (ambient_get_leader(argument0) != noone);
